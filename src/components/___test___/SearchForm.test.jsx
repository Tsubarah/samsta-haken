import { screen, render } from "../../utils/test-utils";
import SearchForm from "../SearchForm";

describe("Tests search form", () => {
	it("render input field", () => {
		// render
		// render(<SearchForm />);
		// find
		// assert
	});
});
