import {
	screen,
	render,
	renderWithBrowserRouter,
} from "../../utils/test-utils";
import RestaurantEditCard from "../RestaurantEditCard";

describe("Tests input fields in edit restaurant card", () => {
	it("render input fields", () => {
		// render
		// renderWithBrowserRouter(<RestaurantEditCard />);
		// render(<RestaurantEditCard />);
		// find
		// assert
	});
});
